import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.louis.weekmealplanner',
  appName: ' week meal planner',
  webDir: 'dist'
};

export default config;
